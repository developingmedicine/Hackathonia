/**
 * Single source of truth for status vocabularies and UI color mapping (PRD §16):
 * CriterionStatus, EnrollmentStatus, status → color/label/icon.
 * Must stay in sync with backend models and validate script (merge rule §34).
 */

/**
 * Typed client for every backend endpoint in PRD §24:
 * trials search/get/parse/knowledge, patients list/get/import,
 * screenings (single + cohort), trial-status patch, follow-up transcript.
 * One function per endpoint; all responses typed against types/index.ts.
 */

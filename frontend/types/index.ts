/**
 * Frontend data types — mirror of PRD §28:
 * EnrollmentStatus, PatientSummary, CriterionResult, EvidenceItem,
 * plus Trial, ParsedCriterion, ClinicianKnowledge, ScreeningResult,
 * WorkupItem, ClinicalFlag, AdverseEvent as needed by the six pages.
 * Merge rule (§34): any schema change updates this file + backend models
 * + validation script + sample JSON + PRD together.
 */

/**
 * PAGE 2 — Trial Intelligence & Clinician Knowledge Intake.
 * Left: parsed inclusion/exclusion criteria with type badges
 *   (Rule / Judgement / Missing-data-sensitive / Human review).
 * Right: voice recording → editable transcript → extracted guidance
 *   → "Apply to Screening Logic". Footer: "Screen Patient Cohort →".
 * Data: GET /api/trials/{id}, POST /parse, POST /knowledge, POST /screen-cohort.
 * PRD: §13 Page 2, §24.2–24.4, §24.9, §25.1 mock clinician voice.
 */

"""
Knowledge intake service: transcript → structured clinician knowledge (§21):
extract triggers (patient-field conditions) and effects (flag/priority/label),
attach to trial, never auto-exclude — flags always require human review.
"""

"""
GET   /api/patients                        — queue list w/ filters      §24.5
GET   /api/patients/{patient_id}           — full patient object        §24.6
POST  /api/patients/import                 — validated bulk import      §24.7
PATCH /api/patients/{patient_id}/trial-status — enrollment updates      §24.10
"""
